local Player = game:GetService("Players").LocalPlayerlocal Camera = workspace.CurrentCameralocal TweenService = game:GetService("TweenService")local RunService = game:GetService("RunService")local UIS = game:GetService("UserInputService")local ScreenGui = Instance.new("ScreenGui", Player:WaitForChild("PlayerGui"))ScreenGui.Name = "RCHub_V2_Ultimate"ScreenGui.ResetOnSpawn = false-- [[ VARIÁVEIS ]]_G.CurrentHitboxScale = 5.0_G.HitboxEnabled = false_G.DirectionalJump = true_G.AirMoveEnabled = false_G.AirMoveSpeed = 50_G.AntiAFK = false-- [[ SETUP CHARACTER ]]local humanoid, hrplocal function setupCharacter(char)    if not char then return end    humanoid = char:WaitForChild("Humanoid")    hrp = char:WaitForChild("HumanoidRootPart")endsetupCharacter(Player.Character)Player.CharacterAdded:Connect(setupCharacter)-- [[ HITBOX LOGIC ]]local function createOrUpdateHitboxes(scale)    for _, model in ipairs(workspace:GetChildren()) do        if model:IsA("Model") and model.Name:match("^CLIENT_BALL_%d+$") then            local ball = model:FindFirstChild("Ball.001")            if not ball then                local ref = nil                for _, p in ipairs(model:GetDescendants()) do if p:IsA("BasePart") then ref = p break end end                if ref then                    ball = Instance.new("Part", model); ball.Name = "Ball.001"; ball.Shape = Enum.PartType.Ball                    ball.Size = Vector3.new(2, 2, 2) * scale; ball.CFrame = ref.CFrame; ball.Anchored = true                    ball.CanCollide = false; ball.Transparency = 0.7; ball.Material = Enum.Material.ForceField; ball.Color = Color3.fromRGB(160, 32, 240)                end            else ball.Size = Vector3.new(2, 2, 2) * scale end        end    endend-- [[ DIRECTIONAL JUMP ]]UIS.JumpRequest:Connect(function()    if _G.DirectionalJump and humanoid and hrp then        task.defer(function()            task.wait(0.03)            local dir = Vector3.new(Camera.CFrame.LookVector.X, 0, Camera.CFrame.LookVector.Z)            if dir.Magnitude > 0 then                hrp.CFrame = CFrame.lookAt(hrp.Position, hrp.Position + dir.Unit)                humanoid.AutoRotate = false            end        end)    elseif humanoid then humanoid.AutoRotate = true endend)-- [[ RENDER LOOP (FPS & AIRMOVE) ]]local fpsLabel = Instance.new("TextLabel", ScreenGui)fpsLabel.Size = UDim2.new(0, 100, 0, 30); fpsLabel.Position = UDim2.new(0, 10, 0, 10); fpsLabel.BackgroundTransparency = 1; fpsLabel.TextColor3 = Color3.fromRGB(160, 32, 240); fpsLabel.Font = Enum.Font.GothamBold; fpsLabel.TextSize = 14; fpsLabel.Visible = falselocal lastUpdate, frameCount = os.clock(), 0RunService.RenderStepped:Connect(function()    frameCount = frameCount + 1    if os.clock() - lastUpdate >= 1 then        if fpsLabel.Visible then fpsLabel.Text = "FPS: " .. frameCount end        frameCount, lastUpdate = 0, os.clock()    end    if _G.HitboxEnabled then createOrUpdateHitboxes(_G.CurrentHitboxScale) end    if _G.AirMoveEnabled and humanoid and hrp then        local state = humanoid:GetState()        if state == Enum.HumanoidStateType.Freefall or state == Enum.HumanoidStateType.Jumping then            local moveDir = humanoid.MoveDirection            if moveDir.Magnitude > 0 then                hrp.Velocity = Vector3.new(moveDir.X * _G.AirMoveSpeed, hrp.Velocity.Y, moveDir.Z * _G.AirMoveSpeed)            end        end    endend)-- [[ INTERFACE - SISTEMA V1 DE ARRASTE ]]local Main = Instance.new("Frame", ScreenGui)Main.Size = UDim2.new(0, 420, 0, 320); Main.Position = UDim2.new(0.5, -210, 0.5, -160); Main.BackgroundColor3 = Color3.fromRGB(10, 10, 10); Main.Active = true; Main.Draggable = true -- SISTEMA V1Instance.new("UICorner", Main); local Stroke = Instance.new("UIStroke", Main); Stroke.Thickness = 2spawn(function() while task.wait() do Stroke.Color = Color3.fromHSV(tick() % 5 / 5, 1, 1) end end)local Sidebar = Instance.new("Frame", Main); Sidebar.Size = UDim2.new(0, 110, 1, 0); Sidebar.BackgroundTransparency = 1local TabButtons = Instance.new("ScrollingFrame", Sidebar); TabButtons.Size = UDim2.new(1, 0, 1, -80); TabButtons.Position = UDim2.new(0,0,0,40); TabButtons.BackgroundTransparency = 1; TabButtons.ScrollBarThickness = 0Instance.new("UIListLayout", TabButtons).Padding = UDim.new(0, 4)local Container = Instance.new("Frame", Main); Container.Size = UDim2.new(1, -125, 1, -20); Container.Position = UDim2.new(0, 120, 0, 10); Container.BackgroundTransparency = 1local Pages = {}local function CreateTab(name)    local Page = Instance.new("ScrollingFrame", Container); Page.Size = UDim2.new(1, 0, 1, 0); Page.BackgroundTransparency = 1; Page.Visible = false; Page.ScrollBarThickness = 0    Instance.new("UIListLayout", Page).HorizontalAlignment = Enum.HorizontalAlignment.Center; Page.UIListLayout.Padding = UDim.new(0,10)    local btn = Instance.new("TextButton", TabButtons); btn.Size = UDim2.new(0.9, 0, 0, 28); btn.BackgroundColor3 = Color3.fromRGB(20, 20, 20); btn.Text = name; btn.TextColor3 = Color3.fromRGB(200, 200, 200); btn.Font = Enum.Font.GothamBold; btn.TextSize = 8; Instance.new("UICorner", btn)    btn.MouseButton1Click:Connect(function() for _, p in pairs(Pages) do p.Visible = false end Page.Visible = true end)    Pages[name] = Page; return Pageend-- ABASlocal TabFarm = CreateTab("FARM")local TabHacks = CreateTab("HACKS")local TabChar = CreateTab("PERSONAGEM")local TabSec = CreateTab("SEGURANÇA")local TabFPS = CreateTab("FPS")local TabHelp = CreateTab("COMO USAR")local TabCred = CreateTab("CREDITOS")-- [[ COMPONENTES ]]local function AddInput(parent, text, callback)    local Frame = Instance.new("Frame", parent); Frame.Size = UDim2.new(0.9, 0, 0, 45); Frame.BackgroundTransparency = 1    local Label = Instance.new("TextLabel", Frame); Label.Size = UDim2.new(1, 0, 0, 15); Label.Text = text; Label.TextColor3 = Color3.fromRGB(255, 255, 255); Label.BackgroundTransparency = 1; Label.Font = Enum.Font.GothamBold; Label.TextSize = 8    local Box = Instance.new("TextBox", Frame); Box.Size = UDim2.new(1, 0, 0, 25); Box.Position = UDim2.new(0, 0, 0, 18); Box.BackgroundColor3 = Color3.fromRGB(30, 30, 30); Box.Text = tostring(_G.CurrentHitboxScale); Box.TextColor3 = Color3.fromRGB(160, 32, 240); Box.Font = Enum.Font.GothamBold; Box.TextSize = 10; Instance.new("UICorner", Box)    Box.FocusLost:Connect(function() local val = tonumber(Box.Text) if val then callback(val) end end)endlocal function AddToggle(parent, text, callback)    local state = false    local btn = Instance.new("TextButton", parent); btn.Size = UDim2.new(0.95, 0, 0, 32); btn.BackgroundColor3 = Color3.fromRGB(25, 25, 25); btn.Text = "[OFF] "..text; btn.TextColor3 = Color3.fromRGB(255, 255, 255); btn.Font = Enum.Font.GothamBold; btn.TextSize = 8; Instance.new("UICorner", btn)    btn.MouseButton1Click:Connect(function() state = not state; btn.Text = (state and "[ON] " or "[OFF] ")..text; btn.TextColor3 = state and Color3.fromRGB(160,32,240) or Color3.fromRGB(255,255,255); callback(state) end)end-- [[ FARM ]]local remote = game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.7.0"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("SeasonService"):WaitForChild("RF"):WaitForChild("RequestRankedReward")AddToggle(TabFarm, "Lucky Spins", function(v) _G.L = v spawn(function() while _G.L do pcall(function() remote:InvokeServer(1) end) task.wait(0.5) end end) end)AddToggle(TabFarm, "Auto Yen", function(v) _G.Y = v spawn(function() while _G.Y do pcall(function() remote:InvokeServer(2) end) task.wait(0.6) end end) end)AddToggle(TabFarm, "Auto Habilidades", function(v) _G.H = v spawn(function() while _G.H do pcall(function() remote:InvokeServer(4) end) task.wait(1.5) end end) end)-- [[ HACKS & CHAR ]]AddToggle(TabHacks, "Ativar Hitbox Bola", function(v) _G.HitboxEnabled = v end)AddInput(TabHacks, "Tamanho Hitbox (Digite)", function(v) _G.CurrentHitboxScale = v end)AddToggle(TabChar, "Directional Jump", function(v) _G.DirectionalJump = v end)AddToggle(TabChar, "Air Movement", function(v) _G.AirMoveEnabled = v end)AddInput(TabChar, "Velocidade Ar", function(v) _G.AirMoveSpeed = v end)-- [[ SEGURANÇA & FPS ]]AddToggle(TabSec, "Anti-AFK", function(v) _G.AntiAFK = v end)AddToggle(TabFPS, "Mostrar FPS", function(v) fpsLabel.Visible = v end)-- [[ COMO USAR ]]local function AddTxt(p, t, s, c) local l = Instance.new("TextLabel", p); l.Size = UDim2.new(1,0,0,25); l.BackgroundTransparency=1; l.Text=t; l.TextColor3=c or Color3.fromRGB(200,200,200); l.Font=Enum.Font.GothamBold; l.TextSize=s or 9; l.TextWrapped = true endAddTxt(TabHelp, "GUIA ROCHA HUB V2", 11, Color3.fromRGB(160,32,240))AddTxt(TabHelp, "1. FARM: O Lucky Spin coleta itens da Season automaticamente.", 8)AddTxt(TabHelp, "2. HITBOX: Digite o valor (ex: 12) para aumentar o alcance da bola.", 8)AddTxt(TabHelp, "3. PULO: Desative o Shift-Lock para o Directional Jump funcionar.", 8)AddTxt(TabHelp, "4. ANTI-AFK: Mantém você no jogo mesmo parado.", 8)AddTxt(TabCred, "FEITO K7 E MANDELA", 14, Color3.fromRGB(160,32,240))AddTxt(TabCred, "K7", 10)-- [[ MINIMIZAR V1 STYLE ]]local Floating = Instance.new("TextButton", ScreenGui)Floating.Size = UDim2.new(0, 50, 0, 50); Floating.Position = UDim2.new(0, 20, 0.5, -25); Floating.BackgroundColor3 = Color3.fromRGB(15, 15, 15); Floating.Text = "K7"; Floating.TextColor3 = Color3.fromRGB(160, 32, 240); Floating.Font = Enum.Font.GothamBold; Floating.Visible = false; Floating.Active = true; Floating.Draggable = trueInstance.new("UICorner", Floating).CornerRadius = UDim.new(1,0)local MinBtn = Instance.new("TextButton", Main); MinBtn.Size = UDim2.new(0, 30, 0, 30); MinBtn.Position = UDim2.new(1, -35, 0, 5); MinBtn.Text = "-"; MinBtn.TextColor3 = Color3.fromRGB(160, 32, 240); MinBtn.BackgroundTransparency = 1; MinBtn.TextSize = 25MinBtn.MouseButton1Click:Connect(function() Main.Visible = false; Floating.Visible = true end)Floating.MouseButton1Click:Connect(function() Floating.Visible = false; Main.Visible = true end)TabHelp.Visible = true; Main.Visible = true
